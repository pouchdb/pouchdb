#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../deflate-js/bin/deflate.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../deflate-js/bin/deflate.js" "$@"
  ret=$?
fi
exit $ret
