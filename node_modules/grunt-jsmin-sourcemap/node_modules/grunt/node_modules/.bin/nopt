#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../nopt/bin/nopt.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../nopt/bin/nopt.js" "$@"
  ret=$?
fi
exit $ret
