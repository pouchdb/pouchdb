#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../jshint/bin/hint" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../jshint/bin/hint" "$@"
  ret=$?
fi
exit $ret
