#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../gzip-js/bin/gzip.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../gzip-js/bin/gzip.js" "$@"
  ret=$?
fi
exit $ret
