#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../nodeunit/bin/nodeunit" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../nodeunit/bin/nodeunit" "$@"
  ret=$?
fi
exit $ret
