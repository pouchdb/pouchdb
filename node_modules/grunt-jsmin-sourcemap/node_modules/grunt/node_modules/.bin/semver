#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../semver/bin/semver" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../semver/bin/semver" "$@"
  ret=$?
fi
exit $ret
