#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../requirejs/bin/r.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../requirejs/bin/r.js" "$@"
  ret=$?
fi
exit $ret
