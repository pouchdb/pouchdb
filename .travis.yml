language: node_js
node_js:
  - 0.8
services:
  - couchdb

branches:
  only:
    - master

before_script:
  - npm install -g grunt-cli
  - rm -r node_modules/pouchdb-server/node_modules/pouchdb
  - cd node_modules/pouchdb-server/node_modules; ln -s ../../.. pouchdb; cd -
