var PouchPromise;
if (process.env.POUCHDB_LITE) {
  PouchPromise = Promise;
} else {
  /* istanbul ignore next */
  PouchPromise = typeof Promise === 'function' ? Promise : require('lie');
}

export default PouchPromise;
