import React from 'react';
import Default from './Default';

export default {
    title: 'Default',
    component: Default,
};

export const ErrorComponent = ({error, reason}) => {
    return (
        <div>
            {error}
            {reason}
        </div>
    );
}
