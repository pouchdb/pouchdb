// this code only runs in the browser, as its own dist/ script

import PouchDBLite from 'pouchdb-lite';

(function (global) {
  global.PouchDB = PouchDBLite;
})(typeof window !== 'undefined' ? window : self);